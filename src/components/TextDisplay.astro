---
// Henter parametrene fra Astro.props
const { htmldisplay="h1", color=""} = Astro.props; 
const Htmldisplay = htmldisplay;

//Definer den styling, der gør sig gældende for de forskellige varianter (farve).
const colors = {
"white": "text-primary-50",
"black": "text-grey-scale-950",
}

// Alt styling sættes på
const colorClasses = `
  font-raleway text-display font-bold uppercase
  ${colors[color] || colors["white"]} 
`; // JavaScript fallbacks: color klassen bruges hvis der skrives en, ellers bliver den hvide (default)

---
<Htmldisplay class={colorClasses}><slot /></Htmldisplay>

