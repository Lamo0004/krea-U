---
// Henter parametrene fra Astro.props
const { htmlheading="h2", size="", stylingclasses=""} = Astro.props; 
const Htmlheading = htmlheading;
 
//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige størrelser.
const sizeClasses = {
"xl": "text-heading-xl text-accent-600",
"l": "text-heading-l text-accent-600",
"m-green": "text-heading-m text-accent-600",
"m": "text-heading-m text-accent-950",
"s": "text-heading-s text-accent-950",
"xs": "text-heading-xs text-accent-950"
};
---
<Htmlheading class:list={[sizeClasses[size], "font-raleway font-semibold uppercase", stylingclasses]}><slot /></Htmlheading>