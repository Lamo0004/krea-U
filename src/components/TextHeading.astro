---
// Henter parametrene fra Astro.props
const { htmlheading="h2", size="", stylingclasses=""} = Astro.props; 
const Htmlheading = htmlheading;
 
//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige størrelser.
const sizeClasses = {
"xl": "text-heading-xl",
"l": "text-heading-l",
"m": "text-heading-m",
"s": "text-heading-s",
"xs": "text-heading-xs",
};
---
<Htmlheading class:list={[sizeClasses[size], "font-raleway font-semibold uppercase text-grey-scale-100 leading-relaxed", stylingclasses]}><slot /></Htmlheading>